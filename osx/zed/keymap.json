// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
      // zed binds cmd+b to toggle left dock
      // but I prefer dock to be right side
      // zed uses cmd+r for this but my muscle memory is cmd+b from vscode
      // swap the toggle keybinds for convenience
      "cmd-b": "workspace::ToggleRightDock",
      "cmd-r": "workspace::ToggleLeftDock",
    },
  },
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      // "j k": "vim::NormalBefore"
    },
  },
  {
    "context": "Pane",
    "bindings": {
      "ctrl-tab": "pane::ActivateNextItem",
      "shift-ctrl-tab": "pane::ActivatePreviousItem",
      "ctrl->": "pane::SwapItemRight",
      "ctrl-<": "pane::SwapItemLeft",
    },
  },
  {
    "context": "Editor",
    "bindings": {
      "cmd-y": "editor::Redo",
    },
  },
]
