#!/bin/bash

# set -x

# View the SHA, description and history graph of last 20 commits
git log --pretty=format:"%C(green)%h %C(white)%<(15,trunc)%cr %C(white)%<(12,trunc)%al %C(auto)%d %<(80,trunc)%s" -n20 --graph --color $@ | nl -w 3
